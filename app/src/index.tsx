import Workspaces from './Workspaces';
import * as ReactDOM from 'react-dom';
import * as React from 'react';

const appEle = document.getElementById("workspace-app");
ReactDOM.render(<Workspaces/>, appEle);
